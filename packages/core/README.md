# @deepdraft/core

Provider-neutral article generation pipeline and agent contract for DeepDraft.

```ts
import { runPipeline, type LLMProvider } from '@deepdraft/core';
```

The package contains no Codex SDK, CLI, filesystem output, or terminal UI dependency.
